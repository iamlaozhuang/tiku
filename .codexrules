# 题库系统全栈品味十诫 (Code Taste Ten Commandments)

本文件定义了针对 Codex 桌面端等 AI 助手参与本项目的全局开发准则。

**请严格遵守以下核心法则，否则你输出的代码将被视为废品：**

1. **前端与 UI 品味**
   - **禁止廉价视觉感**：禁用默认的 `Inter` 字体；禁用纯黑色（`#000000`）；禁用无理由的紫蓝色渐变。
   - **强制交互状态完备性**：必须显式实现 Loading、Empty、Error 状态。
   - **强制微动效物理反馈**：可点击元素必须有弹簧动画反馈（Spring Physics）或按压反馈。
   - **Tailwind 类名秩序**：严格遵循官方 Tailwind 排序规则。

2. **后端与数据库品味**
   - **拒绝 N+1 查询**：Drizzle ORM 中查询关联数据必须使用 Relational Queries (`db.query.*.findMany`)。
   - **强制强类型 Schema 驱动**：严禁业务层手写 SQL 字符串，严禁 `drizzle-kit push` 覆盖数据。
   - **标准化 API 响应契约**：服务端 API 必须始终返回 `{ code, message, data }`。

3. **业务逻辑与代码整洁**
   - **消灭垃圾注释**：禁止写“说明代码在干嘛”的废话注释，只能写“为什么要这么处理”的上下文注释。
   - **有意义的命名法则**：禁止使用 `data`, `res`, `temp`, `x` 等变量名。函数名必须是明确的“动词-名词”结构。
   - **坚持数据不可变性 (Immutability)**：禁止直接修改状态对象或数组的内部属性，必须返回新对象。

**自检要求**：在每次交付代码前，你必须向用户输出一份基于上述规则的 Checklist 合规自检报告！
