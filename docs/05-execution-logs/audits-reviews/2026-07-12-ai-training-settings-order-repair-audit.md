# AI 训练生成设置顺序修复审计

**任务：** `user-led-ai-training-settings-order-repair-2026-07-12`

## 审计结果

无阻断问题。

## 两轮对抗式复核

### 第一轮：行为与边界

- Provider-closed/error 不再把生成设置推到长历史之后。
- Provider-available 原有默认展开和生成流程未改变。
- 个人高级版与企业高级版员工共享修复，但继续根据有效授权上下文显示各自标题和额度归属。
- 关闭状态授权切换、AI出题/AI组卷切换和历史浏览保持可用，生成 POST 继续被阻断。
- 未改变标准版直接 URL、授权、组织、额度或正式题目/试卷边界。

### 第二轮：视觉、无障碍与回归

- 桌面端在模式标签后立即看见“生成设置”，不再被历史详情遮蔽。
- 390px 下设置默认收起、紧跟模式标签，历史随后展示，页面无横向溢出。
- 展开设置后原参数完整存在，disabled 状态和原因文案一致。
- 原生 `details/summary` 键盘语义保留；禁用按钮有可关联的说明文本。
- 360 文件 / 1983 用例、lint、typecheck、format 和 webpack build 全部通过。

## 残余边界

- 生产 Provider 接入和 release readiness 仍是独立任务；本批不以隐藏提示代替生产能力建设。
- 当前服务关闭时移动端仍需展开设置才能看到完整参数，这是明确的空间权衡，不再存在跨越长历史才能发现入口的问题。

## 品味合规自检 Checklist

- [x] 使用现有 token、组件和原生交互语义，未新增颜色、间距魔法数或依赖。
- [x] 未改变 API envelope、数据库、schema、Provider、授权或额度合同。
- [x] 状态说明面向用户，不暴露内部开关、错误码或技术载荷。
- [x] 交互保留 loading/empty/error/closed/available 既有状态，没有删除失败恢复路径。
- [x] 命名明确、状态不可变、无无意义注释、无 N+1 或手写 SQL。
- [x] TDD RED/GREEN、全量门禁、两角色桌面与 390px 浏览器证据完整。

## 结论

APPROVE：修复范围最小、角色边界稳定，适合提交、ff-only 合入 `master`、主分支复验、普通推送及短分支清理。
