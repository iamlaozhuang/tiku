# 0704 企业训练生命周期读取修复对抗审计

## 审计结论

- A02：关闭。异常历史版本不再让管理端生命周期列表整体 500，合法草稿和版本继续显示。
- A03：关闭。异常版本不进入员工可见列表或两类 AI 本地题源。
- 完整性状态只表达 `complete` / `partial`，告警码固定且脱敏；未暴露异常行数量、字段名、内部 ID、授权血缘或原始内容。
- A04 未在本批实现：UI 禁用“新建企业训练”和重试/返回入口仍按 B3 独立处理。

## 对抗矩阵

| 边界        | 攻击/失败形态                   | 结果                                                         |
| ----------- | ------------------------------- | ------------------------------------------------------------ |
| 发布范围    | 缺失范围时补当前组织            | 已阻断；整行隔离，不推断范围                                 |
| 生命周期    | 缺失发布时间时使用创建时间      | 已阻断；整行隔离，不伪造发布时间                             |
| 组织隔离    | 异常行绕过组织上下文            | 已阻断；网关范围过滤保留，安全解析再次拒绝不完整行           |
| 员工可见性  | 一条坏行拖垮全部员工训练        | 已阻断；只返回完整且后续服务判定可作答的版本                 |
| AI 题源     | 异常历史快照进入 AI组卷本地题源 | 已阻断；管理员和员工题源均先通过安全解析                     |
| 敏感信息    | 告警返回异常值或字段明细        | 已阻断；仅固定状态和固定告警码                               |
| 标准/高级版 | 完整性合同扩大高级能力          | 未发生；既有 service-computed capability 和 route guard 未改 |
| 正式内容    | 企业训练读修复写入正式题库/试卷 | 未发生；无正式内容写路径变化                                 |
| Provider    | 验证触发 Provider               | 未发生；Provider 持续关闭                                    |

## 回归审查

- 既有发布、下架、复制、员工答题和详情路径继续使用严格 DTO；写入语义未变。
- 新增列表读取包装器只在真实仓储适配器提供完整性状态；测试注入的旧数组合同被规范化为 `complete`，避免无关调用方回归。
- API 增加字段但不删除或改名；现有管理端和员工端消费者忽略新增字段，B3 再显式消费管理端状态。
- 无依赖、lockfile、schema、migration、seed 或环境文件变化。

## 新发现

- A31 / P1：Next 16 build 类型生成被卡密详情路由的同步/异步 `params` 联合类型阻断。
- 修复批次：B1D，位于 B1C 后、B2 前，独立计划、RED/GREEN、提交和回滚边界。
- 本批不修改该文件，防止把 build 基线问题混入企业训练数据边界修复。

## 品味合规自检 Checklist

- [x] 读取十诫与 ADR，使用既有 contract → mapper → repository → service → route 分层。
- [x] 未以 UI 显隐替代服务端组织和版本边界。
- [x] 未伪造发布范围、发布时间、授权上下文或生命周期状态。
- [x] API 保持标准 envelope 与 camelCase；持久化字段继续 snake_case。
- [x] 未增加硬编码样式、纯黑色或 UI 魔法数值。
- [x] 未新增依赖、schema、迁移、seed、Provider 或敏感日志。
- [x] 测试先复现 RED，再以最小实现得到 GREEN，并执行累计全量回归。
