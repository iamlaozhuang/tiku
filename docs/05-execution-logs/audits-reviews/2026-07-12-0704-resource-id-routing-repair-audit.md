# 0704 资源 publicId 路由兼容修复对抗审计

## 审计结论

- A05：源代码与合同回归关闭。资源 `publicId` 作为不透明值处理，历史下划线标识和 URL 保留字符不再被前端格式正则阻断。
- 详情、校对、发布、重建检索索引、停用和启用使用同一编码路径构造规则。
- localhost 0704DB 交互复核仍延后，不据此声明 staging、production 或 release readiness。

## 对抗矩阵

| 边界       | 攻击/失败形态                       | 结果                                      |
| ---------- | ----------------------------------- | ----------------------------------------- |
| 路径结构   | 标识中的斜杠形成额外路由层级        | 已阻断；整个标识编码为一个路径段          |
| 查询与片段 | 问号或井号改变请求语义              | 已阻断；保留字符按路径段编码              |
| 历史兼容   | 下划线标识被 kebab-only 正则拒绝    | 已关闭；前端只检查非空                    |
| 值完整性   | trim、替换或转 kebab 后请求错误资源 | 未发生；原值只用于编码，不改写            |
| 空标识     | 空路径段发起详情或写请求            | 已阻断；全部操作保持禁用                  |
| 权限       | 客户端兼容修复绕过服务端角色校验    | 未发生；未改 API 或服务端守卫             |
| 状态机     | 任意资源状态直接发布、停用或启用    | 未扩大；既有按钮状态和服务端裁决保留      |
| 敏感信息   | UI、日志或证据展示业务标识          | 已阻断；测试 fixture 与证据均为脱敏构造值 |
| Provider   | 资源操作触发 AI Provider            | 未发生；Provider 持续关闭                 |

## 回归审查

- loading、empty、filtered-empty、error、forbidden 和空标识边界继续通过。
- 资源运行时、权限边界和布局测试通过；B1A 组卷选择器与 B1B 企业训练累计回归通过。
- 无依赖、lockfile、API envelope、数据库、schema、migration 或 seed 变化。
- A31 构建阻断可在未包含 B1C 差异的既有基线上复现，仍由 B1D 独立修复。

## 品味合规自检 Checklist

- [x] 读取十诫、ADR 和资源管理需求入口，沿用现有组件与请求封装边界。
- [x] 不以客户端格式规则解释服务端不透明业务标识。
- [x] 统一路径构造，避免六处重复编码或漏编码。
- [x] 未以 UI 显隐替代服务端角色、资源状态、not-found 和审计边界。
- [x] 未增加硬编码颜色、间距、纯黑色或新 UI 魔法数值。
- [x] 未新增依赖、schema、迁移、seed、Provider 或敏感日志。
- [x] 先用真实失败形态建立 RED，再以最小实现得到 GREEN，并执行累计与全量回归。
