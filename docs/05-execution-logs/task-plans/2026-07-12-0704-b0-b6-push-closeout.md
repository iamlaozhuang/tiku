# 0704 B0-B6 推送收口方案

**日期：** 2026-07-12

**任务：** `0704-b0-b6-push-closeout-2026-07-12`

**分支：** `codex/0704-b0-b6-push-closeout`

**基线：** 本地 `master` 为 `98f5dbd142d8ea71cf759b8dde6ae6d2940bfdfb`，刷新后的 `origin/master` 为 `fa65cbd9bb676d813e627221e23f71a7476e05d8`。

## 批准边界

- 用户在 2026-07-12 明确批准串行执行 push-ready 只读复核和普通 push。
- 唯一允许的远端写操作是 `git push origin master`。
- 禁止 force push、PR、tag、release、staging、production、deploy、Provider、Cost Calibration、env/secret、数据库或正式内容操作。

## 顺序

1. 刷新远端引用，确认本地 `master` 是 `origin/master` 的严格后代且工作区 clean。
2. 核对提交链、变更文件、禁止文件、二进制和高风险凭证模式。
3. 仅更新 push-closeout 的 plan、evidence、audit 和 agent state，提交并快进合入本地 `master`。
4. 执行 Module Run v2 pre-push readiness，重新确认远端没有前进。
5. 执行普通 `git push origin master`，随后核对本地和远端 SHA 一致。
6. 写入远端同步检查点，普通 push 该文档提交并清理短分支。

## 停止条件

- `origin/master` 出现远端领先或不再是本地祖先。
- 发现 env、凭证、私钥、数据库 URL、依赖/lockfile、schema、migration、seed 或非预期二进制变更。
- pre-push readiness、Git 完成度、远端 SHA 核对或普通 push 失败。
- 需要 force、PR、部署、Provider 或其他未批准远端动作。
